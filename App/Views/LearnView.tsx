import { styles } from "../Shares/styles";
import React, { useState } from "react";
import { Button, StyleSheet, Text, View } from "react-native";

export function LearnView({ navigation }: any) {
	return (
		<View style={styles.container}>
			<Text>LearnView</Text>
		</View>
	);
}
